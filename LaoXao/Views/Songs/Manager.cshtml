@model IEnumerable<BusinessObject.Song>

@{
    ViewData["Title"] = "Manager";
    Layout =null;
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css">
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
}

<!DOCTYPE html>
<html>
<head>
    <!-- Bao gồm liên kết Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
   
</head>
<body>
    <!-- Nội dung bố cục của bạn ở đây -->
    <main>
        <h1 style="margin-left: 13%;">Manager</h1>

        <p style="margin-left: 13%;">
            <a asp-action="Create" class="btn btn-primary">Create New</a>
        </p>


        <table class="table align-middle mb-0 bg-black table-striped container">
            <thead class="bg-black">
                <tr>
                    <th>ID</th>
                    <th>Song</th>
                    <th>Status</th>
                    <th>Position</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody class="bg-black">
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="fw-normal"> @Html.DisplayFor(modelItem => item.Id)</td>
                        <td>
                            <div class="d-flex align-items-center">
                                <img src=" @Html.DisplayFor(modelItem => item.ImgUrl)"
                                     alt=""
                                     style="width: 45px; height: 45px"
                                     class="rounded-circle" />
                                <div class="ms-3">
                                    <p class="fw-bold mb-1">  @Html.DisplayFor(modelItem => item.Title)</p>
                                    <p class="text-muted mb-0">@Html.DisplayFor(modelItem => item.Artist)</p>
                                </div>
                            </div>
                        </td>
                        <td>
                            <audio controls>
                                <source src="@item.FilePath" type="audio/mpeg">
                                Trình duyệt của bạn không hỗ trợ phát nhạc.
                            </audio>
                        </td>
                        
                        <td>Senior</td>
                        <td>


                            <button type="button" class="btn btn btn-primary">@Html.ActionLink("Edit", "Edit", new { id = item.Id }) </button>
                            <button type="button" class="btn btn-outline-danger">@Html.ActionLink("Delete", "Delete", new { id = item.Id }, new {@class="text-danger-emphasis"})</button>

                           
                        </td>
                       
                    </tr>

                }

            </tbody>
        </table>
    </main>
   
</body>
</html>

<<<<<<< HEAD
=======
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Artist)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FilePath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImgUrl)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Artist)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FilePath)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ImgUrl)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn-outline-primary" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })
                </td>
                <td>
                    <audio controls>
                        <source src="@item.FilePath" type="audio/mpeg">
                        Trình duyệt của bạn không hỗ trợ phát nhạc.
                    </audio>
                </td>
            </tr>
        }
    </tbody>
</table>
>>>>>>> 1bc1ae69236dff9259ef116ebfec45524f13b27d
